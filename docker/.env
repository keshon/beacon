#
# DEPLOY SETTINGS
#

ALIAS=beacon
HOST=beacon.keshon.ru

# Set GIT to true to pull sources from git repository, local 'src' dir will be used otherwise if was pulled before
GIT=true
GIT_URL=https://github.com/keshon/beacon
